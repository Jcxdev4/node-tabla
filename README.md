# Notas:

Mi Primer Programa usando Node JS

```

     --help       Show help                                           [boolean]
      --version    Show version number                                 [boolean]
  -b, --base       Obtiene la base por la cual vamos a multiplicar
                                                             [number] [required]
  -l, --listar     Opcion para mostrar la tabla en consola
                                                      [boolean] [default: false]
  -h, --hasta      Define hasta donde queremos que sea la tabla
                                                             [number] [required]
  -n, --numero     Numero que queremos dividr              [number] [default: 5]
  -d, --dividendo  divisor por el cual queremos comenzar a dividir
                                                           [number] [default: 2]

```